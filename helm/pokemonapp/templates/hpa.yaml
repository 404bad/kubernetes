apiVersion: v1
kind: Service
metadata:
  name: {{ include "pokemonapp.fullname" . }}
  labels:
    {{- include "pokemonapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pokemonapp.selectorLabels" . | nindent 4 }}
